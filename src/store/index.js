import { createStore } from 'vuex'

export default createStore({
  state: {
    testData: {}
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
  getters: {
    testData: state => state.testData
  }
})
